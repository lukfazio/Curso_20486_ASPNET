@model IEnumerable<UsuarioIndexVM>
@{
    ViewBag.Title = "Listagem de Usuarios";
    ViewBag.SubTitle = "Listagem de Usuarios Cadastados no Sistema!!";

}


<div>
    <a asp-controller="Usuarios" asp-action="AddEdit" class="btn btn-outline-secondary">Adicionar Produto</a>
</div>

<div>
    <table class="table  table-striped table-hover">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Perfis</th>
                <th>Email</th>
                <th>Genero</th>
                <th>Data de Cadastro</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr id="item-@item.Id">
                    <td>@item.Nome</td>
                    <td>@item.Perfis</td>
                    <td>@item.Email</td>
                    <td>@item.Genero</td>
                    <td>@item.DataCadastro.ToShortDateString()</td>
                    <td>
                        <a class="btn btn-outline-success btn-sm" asp-action="AddEdit" asp-controller="Usuarios" asp-route-id="@item.Id">Editar</a>
                        <button type="button" class="btn btn-outline-danger btn-sm" onclick="excluir(@item.Id, '@item.Nome', 'Usuario', '@Url.Action("Excluir","Usuarios")')">Excluir</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<partial name="_modalDel">


    @section scripts{
        <script src="~/js/dell-modal.js"></script>
    }
